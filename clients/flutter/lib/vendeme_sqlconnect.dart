// Entry point del package: re-exporta el SDK generado por SQL Connect
// (el generador emite src/default.dart como entrada del conector `default`).
// lib/src se regenera con `make generate` (firebase dataconnect:sdk:generate).
export 'src/default.dart';
